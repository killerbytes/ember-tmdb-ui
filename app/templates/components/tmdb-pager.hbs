{{#if model}}

  {{#link-to "movie" (query-params list=params.list page=previousPage) class="controls"}}
    <i class="fa fa-chevron-left" aria-hidden="true"></i>
  {{/link-to}}


{{#if firsTwoShow}}
  <ul>
  {{#each firstTwo as |item|}}
    <li>
      {{link-to item (query-params list=params.list page=item)}}
    </li>
  {{/each}}
  </ul>
  |
{{/if}}


{{#if list}}
  <ul>
    {{#each list as |item|}}
    <li>
      {{link-to item (query-params list=params.list page=item)}}
    </li>
    {{/each}}
  </ul>

{{/if}}

{{#if lastTwoShow}}
...
  <ul>
    {{#each lastTwo as |item|}}
    <li>
      {{link-to item (query-params list=params.list page=item)}}
    </li>
    {{/each}}
  </ul>
{{/if}}


  {{#link-to "movie" (query-params list=params.list page=nextPage) class="controls"}}
    <i class="fa fa-chevron-right" aria-hidden="true"></i>
  {{/link-to}}
{{/if}}
