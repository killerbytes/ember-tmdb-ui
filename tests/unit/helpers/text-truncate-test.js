
import { textTruncate } from 'tmdb-ember/helpers/text-truncate';
import { module, test } from 'qunit';

module('Unit | Helper | text truncate');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = textTruncate([42]);
  assert.ok(result);
});

