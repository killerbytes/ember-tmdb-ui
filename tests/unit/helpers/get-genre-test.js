
import { getGenre } from 'tmdb-ember/helpers/get-genre';
import { module, test } from 'qunit';

module('Unit | Helper | get genre');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getGenre([42]);
  assert.ok(result);
});

