
import { imagePath } from 'tmdb-ember/helpers/image-path';
import { module, test } from 'qunit';

module('Unit | Helper | image path');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = imagePath([42]);
  assert.ok(result);
});

